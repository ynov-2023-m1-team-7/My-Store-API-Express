

const redisOptions = {
    host: '172.21.0.1', // replace with your Redis host
    port: 6379, // replace with your Redis port
    password: 'root', // replace with your Redis password
};

module.exports = {
    redisOptions,
};